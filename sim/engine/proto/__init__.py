"""engine.proto: protocol.py split by subject. See sim/ARCHITECTURE.md.

protocol.py itself is a thin re-export shim over this package, kept so
`from engine.protocol import X` goes on working for every X it ever
exported.
"""
